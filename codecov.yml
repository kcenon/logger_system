# Codecov configuration for logger_system
# Reference: https://docs.codecov.com/docs/codecov-yaml

codecov:
  require_ci_to_pass: yes

coverage:
  precision: 2
  round: down
  range: "50...80"
  status:
    project:
      default:
        target: 65%  # Phase 2: Improved baseline with expanded test suite
        threshold: 2%
        if_ci_failed: error
    patch:
      default:
        target: 70%
        threshold: 5%
        if_ci_failed: error

# Gradual improvement plan:
# - Phase 1: 55% project, 60% patch
# - Phase 2 (Current): 65% project, 70% patch
# - Phase 3: 75% project, 80% patch

parsers:
  gcov:
    branch_detection:
      conditional: yes
      loop: yes
      method: no
      macro: no

ignore:
  - "tests/**/*"
  - "benchmarks/**/*"
  - "examples/**/*"
  - "build/**/*"
  - "vcpkg/**/*"
  - "vcpkg_installed/**/*"
  - "common_system/**/*"
  - "thread_system/**/*"

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: no
